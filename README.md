# mnist-cnn-digit-recognition-webapp
CNN-powered handwritten digit recognition web app built with PyTorch and Flask. Users can draw digits in a browser canvas and get real-time predictions from a trained MNIST model.
